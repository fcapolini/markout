{
  "name": "markout",
  "version": "0.1.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "test": "vitest --run",
    "coverage": "vitest --run --coverage",
    "build": "node scripts/build-server.mjs && node scripts/build-client.mjs",
    "check": "tsc --noEmit",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [],
  "author": "Fabrizio Capolini",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@types/acorn": "4.0.6",
    "@types/node": "24.3.0",
    "@vitest/coverage-v8": "3.2.4",
    "esbuild": "0.25.9",
    "prettier": "3.6.2",
    "ts-node-dev": "^2.0.0",
    "vitest": "3.2.4"
  },
  "dependencies": {
    "acorn": "8.15.0"
  }
}
