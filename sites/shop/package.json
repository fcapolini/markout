{
  "name": "@markout-lang/shop",
  "private": true,
  "version": "0.1.1",
  "description": "A catalog, a cart and a purchase: the worked example of an application written in Markout",
  "scripts": {
    "dev": "tsx --tsconfig ../../packages/cli/tsconfig.dev.json server.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "author": "Fabrizio Capolini",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.4.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@markout-lang/express": "^0.8.0",
    "express": "^5.2.1"
  }
}
