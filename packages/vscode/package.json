{
  "name": "markout-vscode",
  "private": true,
  "version": "0.4.0",
  "description": "Markout language support for VS Code: the compiler's diagnostics, in the editor",
  "displayName": "Markout",
  "publisher": "fcapolini",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./dist/client.js",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "vitest run",
    "typecheck": "tsc -b && npm run typecheck:test",
    "typecheck:test": "tsc -p tsconfig.test.json",
    "probe": "node scripts/probe.mjs"
  },
  "author": "Fabrizio Capolini",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/vscode": "^1.90.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@markout/core": "^0.4.0",
    "@volar/language-core": "^2.4.28",
    "@volar/language-server": "^2.4.28",
    "volar-service-html": "0.0.71",
    "vscode-languageclient": "^9.0.1"
  },
  "contributes": {
    "grammars": [
      {
        "scopeName": "text.html.markout",
        "path": "./syntaxes/markout.tmLanguage.json",
        "injectTo": [
          "text.html.basic",
          "text.html.derivative"
        ],
        "embeddedLanguages": {
          "meta.embedded.expression.markout": "javascript"
        }
      }
    ],
    "configuration": {
      "title": "Markout",
      "properties": {
        "markout.docroot": {
          "type": "string",
          "default": "",
          "markdownDescription": "The directory pages are resolved against, so that `/lib.htm` means the same thing here as it does when served. Empty means guess: the nearest ancestor named `markout`, then the nearest one with a `package.json`, then the workspace folder."
        },
        "markout.enable": {
          "type": "string",
          "enum": [
            "auto",
            "always",
            "never"
          ],
          "enumDescriptions": [
            "Look for evidence: a page using markout's own syntax, or a project that depends on markout.",
            "Diagnose every HTML file, whatever the project.",
            "Never diagnose."
          ],
          "default": "auto",
          "markdownDescription": "When to compile HTML files and report what the compiler says. `auto` looks for evidence: a page that uses markout's own syntax (a `<:\u2026>` directive, or an attribute like `:count=${\u2026}`), or a project that depends on markout. It stays silent otherwise, because a `.html` file holding `${\u2026}` is JSP EL or Thymeleaf at least as often as it is a markout page."
        }
      }
    }
  },
  "activationEvents": [
    "onLanguage:html"
  ]
}
