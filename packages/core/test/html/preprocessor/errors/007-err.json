["\"escaping\" belongs to a literal include: it says how a file's TEXT is written out, and without \"as\" the file is read as markup and spliced in, with no text of its own to escape. Add as=\"pre\" (or whatever tag should hold it), or drop \"escaping\""]
